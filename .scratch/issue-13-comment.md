> *This was generated by AI during triage.*

## Agent Brief — R2: WorkspaceAnalysisCache

### Goal

Create `src/services/WorkspaceAnalysisCache.ts` — a TTL-based in-memory cache that wraps `WorkspaceAnalyzer` to avoid redundant workspace scans within a configurable time window.

### Interface

**Constructor**

```ts
constructor(analyzer: WorkspaceAnalyzer, ttlSeconds: number = 300)
```

- Accepts a `WorkspaceAnalyzer` instance and an optional TTL (default 300 seconds).

**Methods**

| Method | Signature | Description |
|--------|-----------|-------------|
| `analyze` | `analyze(workspaceRoot: string): Promise<WorkspaceContext>` | Returns the cached `WorkspaceContext` if the last analysis for `workspaceRoot` is within TTL; otherwise delegates to `WorkspaceAnalyzer.analyze()` and stores the result. |
| `invalidate` | `invalidate(): void` | Clears all cached entries immediately. |

### Unit Tests

Add `src/test/suite/workspaceAnalysisCache.test.ts` covering:

- **Cache hit** — second call within TTL returns the cached result without calling the analyzer again.
- **Cache miss** — first call (no cache) delegates to the analyzer and stores the result.
- **TTL expiry** — a call made after the TTL has elapsed triggers a fresh analysis.
- **invalidate** — calling `invalidate()` forces the next `analyze()` call to re-run the analyzer.

### Definition of Done

- [ ] `src/services/WorkspaceAnalysisCache.ts` compiles clean with `npm run compile`
- [ ] All four unit tests pass with `npm run test`
- [ ] No new ESLint errors (`npm run lint`)

